name: first action
on:
  push:
    branches: ["main"]
  workflow_dispatch:
  
jobs:
  build:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v3
       - name: greet user
         run: echo "wellcome to github actions"
